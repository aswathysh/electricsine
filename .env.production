# Production environment
# API Configuration - make sure SSL certificate matches the domain
NEXT_PUBLIC_API_URL=https://rubiksoftwares.com/electricsine-api/public/api

# Production: Do NOT disable SSL certificate validation
# If you get SSL errors in production, fix the SSL certificate on the Apache server instead
# The certificate CN (Common Name) must match "rubiksoftwares.com/electricsine-api/public" exactly
